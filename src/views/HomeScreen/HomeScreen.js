import React from 'react';
import { Text, View } from 'react-native';
import styles from './style';

export default LoginTypeView = (props) => {

    return (
        <View style={styles.container}>
          <Text>Validar carona</Text>
        </View>
    );
}